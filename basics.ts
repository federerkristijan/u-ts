// Primitives
// ':' type assignment && '=' value assignment
let age: number = 42;

// capital String points at JS Object, small string points at type
let userName: string;

let isReal: boolean;

isReal = true;
